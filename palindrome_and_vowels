#1
# Write a function is_palindrome(s) 
# that checks if a given string s is a palindrome. 
# A palindrome is a word or phrase that reads the same backward as forward.
def is_palindrome(s):
    if s == s[::-1]:
        print ('a')
        print(s)
    else:
        print('b')
        print(s)
    pass

is_palindrome('madam')

#2
#Count Vowels in a String
#Write a function count_vowels(s) that takes a string
#s and returns the number of vowels (a, e, i, o, u) in the string.
vowels = ['a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U']
words = 0
def count_vowels(word):
    global words
    for l in word:
        if l in vowels:
            words += 1 
    print(words)

count_vowels('Alibaba')
